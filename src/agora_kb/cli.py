"""`agora` CLI entry point.

Planned subcommands (docs/ROADMAP.md Phase 1): serve (MCP/web faces), curate (run a consolidation),
repo init (scaffold a knowledge repo with the schema), harvest (run connectors), doctor (health check).
"""

def main() -> int:
    raise SystemExit("agora: not implemented yet — see docs/ROADMAP.md (Phase 1).")
